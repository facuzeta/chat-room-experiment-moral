function featuresMoralTiempos2 = importfileMoralTimes(filename, startRow, endRow)
%IMPORTFILE1 Import numeric data from a text file as a matrix.
%   FEATURESMORALTIEMPOS2 = IMPORTFILE1(FILENAME) Reads data from text file
%   FILENAME for the default selection.
%
%   FEATURESMORALTIEMPOS2 = IMPORTFILE1(FILENAME, STARTROW, ENDROW) Reads
%   data from rows STARTROW through ENDROW of text file FILENAME.
%
% Example:
%   featuresMoralTiempos2 = importfile1('featuresMoralTiempos2.csv', 2, 25885);
%
%    See also TEXTSCAN.

% Auto-generated by MATLAB on 2022/09/28 12:16:20

%% Initialize variables.
delimiter = ',';
if nargin<=2
    startRow = 2;
    endRow = inf;
end

%% Format for each line of text:
%   column1: double (%f)
%	column2: double (%f)
%   column3: categorical (%C)
%	column4: double (%f)
%   column5: double (%f)
%	column6: double (%f)
%   column7: double (%f)
%	column8: double (%f)
%   column9: double (%f)
%	column10: categorical (%C)
% For more information, see the TEXTSCAN documentation.
formatSpec = '%f%f%C%f%f%f%f%f%f%C%[^\n\r]';

%% Open the text file.
fileID = fopen(filename,'r');

%% Read columns of data according to the format.
% This call is based on the structure of the file used to generate this
% code. If an error occurs for a different file, try regenerating the code
% from the Import Tool.
dataArray = textscan(fileID, formatSpec, endRow(1)-startRow(1)+1, 'Delimiter', delimiter, 'TextType', 'string', 'EmptyValue', NaN, 'HeaderLines', startRow(1)-1, 'ReturnOnError', false, 'EndOfLine', '\r\n');
for block=2:length(startRow)
    frewind(fileID);
    dataArrayBlock = textscan(fileID, formatSpec, endRow(block)-startRow(block)+1, 'Delimiter', delimiter, 'TextType', 'string', 'EmptyValue', NaN, 'HeaderLines', startRow(block)-1, 'ReturnOnError', false, 'EndOfLine', '\r\n');
    for col=1:length(dataArray)
        dataArray{col} = [dataArray{col};dataArrayBlock{col}];
    end
end

%% Close the text file.
fclose(fileID);

%% Post processing for unimportable data.
% No unimportable data rules were applied during the import, so no post
% processing code is included. To generate code which works for
% unimportable data, select unimportable cells in a file and regenerate the
% script.

%% Create output variable
featuresMoralTiempos2 = table(dataArray{1:end-1}, 'VariableNames', {'group_id','question_id','stage','participant_id','seconds','n_words','words_length__mean','words_length__median','words_length__std','consensus_reached'});

